<!-- What it changes, and which promise it keeps or repairs — a requirement identifier
     (`req-…`) or a component card row, so the review starts from the promise and not from
     the diff. -->

**The gates that ran.** Paste the verdict line of each gate the change touches (`npx nx
affected -t lint test build typecheck check-package …` — the whole line is in
`.github/workflows/ci.yml`, and `CONTRIBUTING.md` says how to run it here):

```

```

- [ ] The change is contributed under the MIT License, as [CONTRIBUTING.md](../CONTRIBUTING.md) says.
- [ ] A visual change refreshed its baselines, and the diff of the screenshots is in the commit.
