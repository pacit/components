/**
 * TestBed environment for the cost run — assembled by hand, the way the library's mutation
 * run and the sandbox's specs do it, because no `@angular/build` builder stands between
 * Vitest and these files to do it for us.
 */
import '@angular/compiler';
import { setupTestBed } from '@analogjs/vitest-angular/setup-testbed';

setupTestBed();
