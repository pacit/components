{
  "check": "tests",
  "rule": "tests-unmeasured",
  "// defect": "The report lists not one test file. `testFiles` is rendered from the dry run's own results, so an empty one means the run executed no spec at all — and every point below would then rule on the specs of a measurement that never happened.",
  "testFiles": []
}
