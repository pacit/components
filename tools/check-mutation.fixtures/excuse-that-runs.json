{
  "check": "tests",
  "rule": "excuse-that-runs",
  "// defect": "A spec excused as one the mutation run never executes is listed in the report, which means it ran. The entry has outlived its reason, and from here on it would excuse that spec's real absence too — which is the one thing point 3 exists to catch.",
  "policy": {
    "coversNothing": [
      {
        "spec": "libs/fake/epsilon.spec.ts",
        "reason": "Its only subject is the fake library's own test helper, which `patterns` strikes out of the measurement — so nothing it reaches is mutated, the run never selects it, and the report lists it nowhere."
      },
      {
        "spec": "libs/fake/alpha.spec.ts",
        "reason": "Written when the run did not select this spec, and left standing after it grew a subject inside the measurement."
      }
    ]
  }
}
