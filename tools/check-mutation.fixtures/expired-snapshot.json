{
  "check": "score",
  "rule": "expired-snapshot",
  "// defect": "The snapshot has a row for a file outside the measurement. It reads as proof that something is measured — and it is not.",
  "// columns": "The row adds up — 9 killed of 9+1 — because `columns-adrift` runs before this rule and would otherwise take the case away from it.",
  "addSnapshotRow": ["libs/fake/gamma.ts 90.00 9(0) 1 0 0 0"]
}
