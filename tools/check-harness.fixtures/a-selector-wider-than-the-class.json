{
  "point": 2,
  "check": "host",
  "why": "PctButtonHarness also answers to a[pctButton], which the component does not — a test finds an anchor the directive never touched.",
  "harnesses": {
    "PctButtonHarness": { "hostSelector": "button[pctButton], a[pctButton]" }
  }
}
