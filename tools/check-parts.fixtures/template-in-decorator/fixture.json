{
  "point": 1,
  "check": "denominator",
  "$reason": "A template written into the decorator. The source scanner reads template files, so parts written here are seen only by the reading from the package — that is, as a drift between two lists rather than as what they are."
}
