{
  "point": 1,
  "check": "denominator",
  "drop": ["libs/components/widget/src/base.ts.txt"],
  "$reason": "A directive whose base the scan cannot see. Angular merges a decorated base's host attributes into the subclass's definition, so the package read finds `widget-base` on `PctMarker` while the source read has never seen it \u2014 point 2 would then compare the half declared here against the whole of what ships, and report the base's part as one brought in by syntax the scanner is blind to (lesson-100)."
}
