# @pacit/components

The npm page with the widget's row gone — the exact drift the repository lived with for
six components running: the package grows and the page does not.

## Entrypoints

| entrypoint          | what is in it |
| ------------------- | ------------- |
| `@pacit/components` | configuration |
