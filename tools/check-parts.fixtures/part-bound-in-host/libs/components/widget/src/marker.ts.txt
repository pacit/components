import { Directive } from '@angular/core';
import { PctMarkerBase } from './base';

@Directive({
  selector: '[pctMarker]',
  host: {
    class: 'pct-marker',
    'data-pct-part': 'widget-marker',
    '[attr.data-pct-part]': 'part()',
  },
})
export class PctMarker extends PctMarkerBase {}
