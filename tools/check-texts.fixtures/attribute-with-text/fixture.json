{
  "point": 3,
  "check": "template",
  "rule": "speaking-attribute",
  "description": "A `title` with text written straight in. The value of a speaking attribute is read by the user — unlike `role=\"group\"` or `type=\"button\"`, which are keywords of the specification and therefore do not fire. The entry in `package.json` is here so that the two readings agree."
}
