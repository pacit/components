{
  "point": 5,
  "check": "channel",
  "rule": "dead-key",
  "description": "A field declared, given a default value, and read by no component. This is coverage that is not there: it stands in a public type, the consumer translates it, and it is nowhere to be seen. The same move as removing the dead `--pct-on-danger`."
}
