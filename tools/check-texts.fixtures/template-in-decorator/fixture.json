{
  "point": 1,
  "check": "denominator",
  "rule": "template-in-decorator",
  "description": "The component takes its template from the decorator instead of a file. This scanner reads `.html` files, so any text written into a decorator would be invisible to it — and it travels to the browser just the same. This is not a theoretical case."
}
