{
  "point": 1,
  "check": "denominator",
  "rule": "template-without-file",
  "description": "A `templateUrl` pointing at a file that is not on the gate's list. A template outside the list contributes not one node to the measurement, so its texts are invisible to point 3 — and the component renders normally."
}
