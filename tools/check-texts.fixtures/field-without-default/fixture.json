{
  "point": 5,
  "check": "channel",
  "rule": "key-without-default",
  "description": "A field in `PctTexts` with no default value. Overriding is partial (decision 0007), so a field with no default reaches the DOM as `undefined` for everybody who has not translated that one text."
}
