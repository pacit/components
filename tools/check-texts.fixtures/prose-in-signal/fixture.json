{
  "point": 4,
  "check": "typescript",
  "rule": "prose-in-factory",
  "description": "Text in a signal's default value. In a template it is position that decides whether a literal is text; in TS there is no position, so shape decides — a capital letter at the start or a space in the middle. Measured over the whole library: that separates the texts from the axis values without an exception."
}
