{
  "point": 4,
  "check": "typescript",
  "rule": "text-at-construction",
  "description": "An input's default value reads `PCT_TEXTS`. This is not an invented case: that is exactly how `placeholder` stood in `PctSelect` until decision 0014. A default value is produced once, at construction, so an application switching the texts at runtime keeps the old one."
}
