{
  "point": 2,
  "check": "artifact",
  "rule": "attribute-only-in-package",
  "description": "The package carries an `aria-label` on the host, and in the sources it is in no `host` block a regex can read. This is what an attribute brought in by spreading somebody else's object looks like (`...fitHost` in `field/src/affix.ts` — the library really does that)."
}
