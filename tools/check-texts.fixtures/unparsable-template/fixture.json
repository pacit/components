{
  "point": 1,
  "check": "denominator",
  "rule": "template-parser",
  "description": "An `@if` block with no closing brace. Angular's parser rejects such a template, and a tree that does not exist has not one text node either: point 3 would pass over it without objection. The `errors` from `parseTemplate` are the only signal here."
}
