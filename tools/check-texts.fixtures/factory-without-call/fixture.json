{
  "point": 4,
  "check": "typescript",
  "rule": "empty-measurement",
  "description": "`= input` with no call — the scanner finds no bracket and skips the entry, while the independent counter of assignments sees it. Without that pair an input written in a syntax the scanner does not understand (a generic with a `>` inside, a multi-line form) would fall out of the measurement in silence."
}
