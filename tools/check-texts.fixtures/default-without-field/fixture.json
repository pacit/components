{
  "point": 5,
  "check": "channel",
  "rule": "default-without-key",
  "description": "A default value with no field in `PctTexts`. A text that is not in the type is a text the consumer has no way of overriding: `providePctTexts` takes a `Partial<PctTexts>`, so a key outside the interface is a compilation error at their end."
}
