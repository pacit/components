{
  "point": 1,
  "check": "denominator",
  "rule": "template-without-owner",
  "description": "An `.html` file no `templateUrl` points at. The scanner assigns texts to a class through `templateUrl`; a template with no owner is invisible to that assignment, and the commonest cause is worse than an orphan."
}
