{
  "point": 1,
  "check": "denominator",
  "rule": "empty-list",
  "description": "An input with not one decorator — only `texts.ts` is left. The case does not examine a component, it examines the MEASUREMENT: with an empty set of classes every later point passes, ruling on nothing.",
  "drop": ["libs/components/widget"]
}
