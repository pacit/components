{
  "point": 2,
  "check": "artifact",
  "rule": "attribute-only-in-sources",
  "description": "The sources carry a static `aria-label` in a `host` block and the package does not know about it. The commonest cause is a stale `dist` — and this is the case in which point 3 would rule on text the consumer does not get."
}
