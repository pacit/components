{
  "point": 2,
  "check": "artifact",
  "rule": "empty-package",
  "description": "A built package with not one class carrying an Angular definition. The comparison of the two readings then always passes, because one side is empty — and empty equals everything that is not in it. This is what a stale or cleaned `dist` looks like."
}
