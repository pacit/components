{
  "point": 6,
  "check": "warnings",
  "rule": "warning-without-devmode",
  "description": "A warning with no `isDevMode()` guard — neither in its own function nor at the call site. The requirement reserves this channel for development mode, because in production it is a line in the console of an application whose author cannot switch it off."
}
