{
  "point": 1,
  "check": "denominator",
  "rule": "decorator-parser",
  "description": "A `@Directive({` decorator indented by two spaces. Prettier does not format that way, but this gate's parser anchors on exactly column zero — and the independent counter does NOT repeat the anchor. Without that pair the component would drop out of the measurement quietly."
}
