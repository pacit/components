{
  "point": 5,
  "check": "channel",
  "rule": "empty-measurement",
  "description": "The `PctTexts` interface written so that the scanner sees no fields in it. The whole of point 5 compares three lists (fields, defaults, reads) — and with the first one empty it passes, having nothing to compare, and says nothing about dead keys either."
}
