{
  "point": 3,
  "check": "template",
  "rule": "literal-in-expression",
  "description": "Text given as a literal inside a binding expression. It bypasses the channel exactly as text written into a node does, with the difference that it looks like code — and `aria-label` is the one place where an error of this class is seen only by a screen-reader user."
}
