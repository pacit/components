{
  "point": 4,
  "check": "forwarded",
  "$reason": "Two named elements standing side by side, which is what point 4 has always been about: a screen reader meets two controls where the consumer wrote one name, and neither of them is wrong on its own. The rule now counts carriers per DOM state, so this is the input that says the widening did not turn into a shrug — two branches of one conditional pass, two neighbours do not."
}
