import { Component, input } from '@angular/core';

/** A bar whose value a consumer has no way to name. */
@Component({
  selector: 'pct-widget',
  templateUrl: './widget.html',
  host: {
    class: 'pct-widget',
  },
})
export class PctWidget {
  readonly value = input(0);
}
