{
  "point": 2,
  "check": "host",
  "$reason": "A name written by US onto a roleless host — the same defect as a consumer's `aria-label` on the tag, only committed one floor lower, where it looks like a solution."
}
