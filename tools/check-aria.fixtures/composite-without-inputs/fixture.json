{
  "point": 3,
  "check": "inputs",
  "$reason": "The state `pct-tabs` would have been in before the composite roles were added to the widget test. The strip is named as a whole and takes no focus — the keyboard lands on the `<button role=\"tab\">` children — so the old proxy for \"carries a role\", a `tabindex`, saw nothing here: zero widgets, no name inputs asked for, and a tablist a consumer cannot name passing green. Nothing else in the file is unusual, which is the point."
}
