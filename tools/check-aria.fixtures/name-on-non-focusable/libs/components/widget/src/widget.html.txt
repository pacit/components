<span
  class="pct-widget__box"
  [attr.aria-label]="ariaLabel() || null"
  [attr.aria-labelledby]="ariaLabelledby() || null"
>
  <button class="pct-widget__trigger" type="button" role="combobox">
    {{ label() }}
  </button>
</span>
