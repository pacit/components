{
  "point": 1,
  "check": "denominator",
  "$reason": "A component whose base the scan cannot see. Half its surface — fifteen inputs in the case this rule was written from — is declared in a file the gate never read, so every point below would examine the half that happens to be here and pronounce on the whole."
}
