# fixture — a plan with no task in it

## 5. Gaps with no deadline

The prose survives and the list does not. `req-fixture-unbuilt` is named here in running
text, which carries no mark and is therefore no claim about it.

The notation this file would use, quoted rather than used:

```markdown
- [ ] **1 — `req-fixture-unbuilt`**: a task inside a fence is an example of a task
```

A fenced block is blanked out before either count is taken, so the line above is not a task
and cannot make the two counts disagree. This plan has nothing for point 8 to compare.
