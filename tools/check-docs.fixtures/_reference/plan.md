# fixture — a plan that agrees with the registry

<!--
  Point 8's reference plan. Every claim it makes is the state the requirement is really in,
  so the point must find nothing here; each case beside it is this file with one line
  changed. The second item also carries a citation in its BODY, which the point must not
  read — a plan that mentions a requirement has not thereby claimed to close it.
-->

## 5. Gaps with no deadline

- [x] **1 — `req-fixture-shipped`**: the gate this item asked for, built and recorded

  - the requirement's own state has left the gap list, which is what the tick means

- [ ] **2 — `req-fixture-unbuilt`**: a gate nobody has built yet

  - the requirement is still a gap, which is what the empty box means
  - `req-fixture-shipped` is named here, in the body, as context — this point must not read
    it, or every finding that discusses a requirement would be reported as a stale task
