{
  "$comment": "Entrypoint wejścia wzorcowego. Bramka czyta stąd wyłącznie NAZWĘ katalogu — token `--pct-button-*` jest zgadywalny tylko wtedy, gdy `button` jest tym samym słowem, którym konsument importuje komponent.",
  "lib": { "entryFile": "src/index.ts" }
}
