// A types file holding no type: the list of variants, and the union derived from it elsewhere.
// The repository has one of these — `select.types.ts` exports two filter functions — which is
// why the name on a file is not what point 10 measures.
export const fxButtonVariants = ['solid', 'outline'];
