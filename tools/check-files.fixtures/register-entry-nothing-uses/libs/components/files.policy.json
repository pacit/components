{
  "// why": "The register of the file-structure gate, with an entry that has outlived the exception it was written for.",
  "inline": [
    {
      "file": "libs/components/field/src/text.ts",
      "class": "FxText",
      "member": "template",
      "reason": "The host IS the native input: the component projects nothing and renders nothing of its own, so its template is the empty string. A file holding it would hold no characters, and a reader opening `text.html` to see what the control draws would learn less than the decorator tells them."
    },
    {
      "file": "libs/components/button/src/button.ts",
      "class": "FxButton",
      "member": "template",
      "reason": "Written while the button's template was still a string in the decorator, and left standing after it was moved into `button.html` in the same afternoon."
    }
  ]
}
