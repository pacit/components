{
  "point": 5,
  "check": "build",
  "rule": "build-failed",
  "description": "The consumer application's build ended in an error — this is where an unreachable type, a wrong condition in the exports map or a stylesheet the bundler did not resolve land.",
  "buildCode": 1
}
