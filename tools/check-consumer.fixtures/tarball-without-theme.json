{
  "point": 1,
  "check": "tarball",
  "rule": "theme-missing",
  "description": "The theme is in dist and is not in the archive — that is, npm pack filtered it out (files, .npmignore). Exactly the difference `check-package` cannot see by design: it walks the directory, the consumer gets the archive.",
  "dropFile": "themes/pct.css"
}
