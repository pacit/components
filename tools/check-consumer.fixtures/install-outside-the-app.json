{
  "point": 3,
  "check": "install",
  "rule": "outside-the-app",
  "description": "The application resolves the package to the repository's node_modules, not to its own. Searching up the tree gives @angular/* and would hand over @pacit/components by the same route — we would then be measuring a package this gate did not publish.",
  "resolution": "/repository/node_modules/@pacit/components/fesm2022/pacit-components-button.mjs"
}
