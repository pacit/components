{
  "point": 1,
  "check": "tarball",
  "rule": "licence-missing",
  "description": "The LICENSE file is in `dist` but dropped out of the archive — that is how the `files` field or `.npmignore` works. `check-package` reads the directory and sees the full set; the consumer gets a package with no licence, which is exactly what a legal department looks for first.",
  "dropFile": "LICENSE"
}
