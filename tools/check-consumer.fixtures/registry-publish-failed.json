{
  "point": 2,
  "check": "registry",
  "rule": "publish",
  "description": "npm publish ended in an error. Without this point the install below would go to the npmjs uplink for the package and the gate would measure somebody else's artefact.",
  "published": false
}
