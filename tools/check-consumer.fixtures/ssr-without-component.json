{
  "point": 6,
  "check": "ssr",
  "rule": "no-component",
  "description": "There is no class from the component's host block in the HTML from the server — the directive did not match, or the component did not render on the server side.",
  "ssrMarker": "pct-button"
}
