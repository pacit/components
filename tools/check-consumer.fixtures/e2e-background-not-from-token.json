{
  "point": 7,
  "check": "e2e",
  "rule": "background-not-from-token",
  "description": "The theme is loaded and the button paints itself with something other than its own token. Overriding the token at a consumer then changes nothing, and the contrast gate rules on a colour nobody sees.",
  "background": "rgb(255, 0, 0)"
}
