{
  "$comment": "The reference input is the repository itself: the report the cost run writes to tmp/bench/report.json, the demo registry and the record on disk, as they stand at the commit under test. Nothing is stored here on purpose — a stored copy would measure the repository as it was the day somebody stored it. Every case is built on a copy of that live input with one thing broken (see README.md)."
}
