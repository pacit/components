{
  "point": 2,
  "check": "window",
  "description": "A comparator range instead of a caret. The gate refuses rather than guesses: a half-understood range would report a window nobody declared, and point 2 would be comparing the policy against the parser.",
  "peers": { "@angular/core": ">=21.0.0 <23.0.0" }
}
