{
  "// why": "Configuration for the TypeScript files sitting at the repository root — today `vitest.config.ts` and `vitest.workspace.ts`. They belong to no library and no application, so until recently NO compiler saw them: `req-quality-typecheck` promises there is no such code in the workspace (lesson-42). The name is `tsconfig.root.json` and not `tsconfig.json`, because a file under the latter name at the root takes over the whole repository in the editor and in every tool that searches up the tree.",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "target": "es2022",
    "module": "preserve",
    "moduleResolution": "bundler",
    "types": ["node"],
    "strict": true,
    "noImplicitOverride": true,
    "noPropertyAccessFromIndexSignature": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true
  },
  "// include": "A pattern, not a list of names: a file added at the root should come under the compiler by itself. The single star is deliberate — it covers the root alone, so `tools/foo.ts` does NOT slip in here quietly; it fires `check-typecheck` and forces a decision about which project it belongs to. The one decision taken below the root: the package gate's prepared `.d.ts` (`tools/check-package.fixtures/*/types/`) are TypeScript, they belong to the root project that owns `tools/`, and they enter this program — a fixture of the shipped types has to keep the extension the gate reads.",
  "include": [
    "*.ts",
    "*.mts",
    "*.cts",
    "tools/check-package.fixtures/*/types/*.d.ts"
  ]
}
