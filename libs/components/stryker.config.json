{
  "$schema": "../../node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "packageManager": "npm",
  "testRunner": "vitest",
  "vitest": {
    "configFile": "libs/components/mutacja.vitest.config.mts"
  },
  "coverageAnalysis": "perTest",
  "ignorers": ["angular"],
  "mutate": [
    "libs/components/core/src/*.ts",
    "libs/components/field/src/number.ts",
    "libs/components/select/src/select.ts",
    "!libs/components/**/index.ts",
    "!libs/components/**/*.types.ts",
    "!libs/components/**/*.spec.ts"
  ],
  "tempDirName": "tmp/stryker",
  "// ignorePatterns": "Stryker copies the repository into a sandbox and walks it ITSELF, not the git index: without this list it saw 3907 files where git knows 708. The excess is generated directories, and those have concurrent writers — `nx affected` runs `build` beside `mutacja`, ng-packagr creates and deletes `tmp/libs/components/build/tsconfig.generated.<uuid>.json`, and Stryker falls over on `ENOENT ... copyfile` halfway through copying. The symptom is misleading at that: the task is flaky and passes on its own, so the first suspect was the mutant timeout. In passing, the sandbox stops copying its own previous sandboxes from `tmp/stryker`.",
  "ignorePatterns": [
    "tmp",
    "dist",
    "coverage",
    ".nx",
    ".angular",
    "libs/components/themes"
  ],
  "reporters": ["progress", "clear-text", "json"],
  "jsonReporter": {
    "fileName": "tmp/mutacja/mutation.json"
  },
  "htmlReporter": {
    "fileName": "tmp/mutacja/mutation.html"
  },
  "thresholds": {
    "high": 90,
    "low": 80,
    "break": 80
  }
}
