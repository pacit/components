{
  "name": "components",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/components",
  "prefix": "pct",
  "projectType": "library",
  "// release": "Bez `manifestRootsToUpdate`: pakiet powstaje PO podbiciu wersji (tools/release.mjs), więc dist niesie właściwą wersję z samego builda. Łatanie manifestu w dist było potrzebne tylko przy odwrotnej kolejności — i było półśrodkiem, bo poprawiało jeden plik, a nie stałą wkompilowaną w bundle.",
  "release": {
    "version": {
      "currentVersionResolver": "git-tag",
      "fallbackCurrentVersionResolver": "disk"
    }
  },
  "tags": ["type:lib"],
  "// implicitDependencies": "Pakiet wozi skórkę wygenerowaną przez `tokens`, ale nie importuje z niej niczego w TS — bez tego wpisu graf nie zna tej krawędzi i build startuje przed wygenerowaniem CSS.",
  "implicitDependencies": ["tokens"],
  "// themes": "ng-packagr nie czyta assetów spoza katalogu projektu, więc skórka musi najpierw stać się wejściem pakietu. `themes/` jest generowane i gitignorowane — jedynym źródłem prawdy pozostają pliki DTCG w libs/tokens/src.",
  "// schematics": "Kompilowane osobno i PO ng-packagr, bo ng-packagr czyści dist na starcie — build schematics wpięty wcześniej zostałby skasowany. Kolekcja migracji jest w pakiecie od pierwszego wydania celowo: `ng update` czyta ją z wersji ZAINSTALOWANEJ, więc dopisanie jej dopiero przy pierwszej zmianie łamiącej nie pomogłoby nikomu, kto zainstalował wcześniej. Ostatnia kopia (`package.dist.json` → `schematics/package.json`) zamyka granicę modułów: manifest pakietu niesie `\"type\": \"module\"` od ng-packagr, a te pliki są CommonJS-em (`exports.ngAdd = …`), więc bez własnego `{ \"type\": \"commonjs\" }` Node czyta je jako ESM i `ng add @pacit/components` kończy się `exports is not defined in ES module scope` — u konsumenta, przy pierwszej komendzie, jaką wpisze. `check-package` tego nie widzi z założenia: pyta, czy plik fabryki ISTNIEJE, a nie czy da się go wczytać. Plik nazywa się `package.dist.json`, a nie `package.json`, bo prawdziwy manifest w drzewie repozytorium JEST dla Nx projektem (`lekcja-44`). Tak samo rozwiązuje to `@angular/cdk`.",
  "// test": "Wzorce `coverageInclude` idą od korzenia repozytorium, nie od katalogu projektu — mimo tego, co mówi schemat executora: wersja `**/src/**` wciągnęła do raportu biblioteki cały `apps/sandbox`. Szablony są tam razem z kodem, bo logika `@if`/`@for` jest kodem, a bez tej pozycji wypadają z raportu w całości. Powody wyjątków z `coverageExclude` trzyma `tools/check-coverage.mjs` (stała POMIJANE) — to ona decyduje, czego bramka WYMAGA w raporcie, a ta lista musi być jej podzbiorem: gdyby wykluczyła cokolwiek ponadto, plik wypadłby z raportu i zapalił punkt 3.",
  "// check-coverage": "Źródła są w `inputs` WPROST, mimo że raport przychodzi z `test`: plik, którego v8 nie potrafi doliczyć, nie zmienia raportu ani o bajt, więc sam `dependentTasksOutputFiles` dałby trafienie w cache i bramka nie zapaliłaby na jedynej rzeczy, dla której powstała. Komentarze stoją TUTAJ, a nie w targecie: `options` executora ma `additionalProperties: false`, a klucz `// …` w `targets` wywala graf (`lekcja-43`).",
  "// check-zoneless": "Źródła są w `inputs` WPROST, obok wyjścia builda, bo punkty 4 i 6 czytają je BEZPOŚREDNIO — a klucz cache ma wymieniać to, co bramka czyta, nie to, co zwykle się przy okazji zmienia. Zmierzone: jawne `standalone: true` w dekoratorze zostawia `ɵɵngDeclareComponent` bez zmiany (deklaracja i tak niesie `isStandalone: true`) i rusza wyłącznie `ɵɵngDeclareClassMetadata`, czyli echo dekoratora dla debugowania. Cache unieważniłby się więc dziś także bez tej pozycji — ale za sprawą funkcji diagnostycznej, która nie jest niczyją obietnicą. Dzień, w którym ng-packagr przestanie ją emitować, byłby dniem, w którym punkt 6 po cichu przestaje zapalać (`lekcja-44` w trzecim przebraniu). Wzorzec `**/package.json` obejmuje manifesty każdego projektu, także tego, który dopiero powstanie: bramka czyta je z indeksu gita, więc lista wpisana tutaj na sztywno rozjechałaby się przy pierwszej nowej bibliotece.",
  "// check-styles": "Bramka arkuszy: właściwości logiczne i zakaz `opacity` kompozytującej. Nie zależy od `build` — czyta ŹRÓDŁA, bo tylko w nich stoją znaczniki wyjątków i tylko one mają numery linii, którymi da się kogoś nawigować. Wyjście sassa powstaje w samej bramce, z tych samych plików. `inputs` wymieniają arkusze (materiał), źródła TS (mianownik z punktu 3 — komponent stylujący się poza arkuszem) i fixtures. Wzorce są globalne w obrębie projektu, nie listą nazw: nowy entrypoint ma być objęty od pierwszego commita, a lista wpisana na sztywno rozjechałaby się przy pierwszym i to bramka przestałaby wtedy widzieć, a nie CI zapaliło.",
  "// check-bundle": "Bramka tree-shakingu i budżetu rozmiaru. Stoi w `components`, a nie w projekcie roota, bo wszystko, co czyta, leży w bibliotece i w jej `dist` — a nowy entrypoint jest zmianą w `libs/components`, więc czyni ten projekt affected od pierwszego commita. `inputs` wymieniają manifesty entrypointów (lista, po której chodzi pomiar), snapshot (druga strona porównania), skrypt i fixtures. `dependsOn` na `build` jest twarde: bramka bundluje ARTEFAKT, nie źródła, bo to artefakt dostaje konsument — sondy sięgają po pakiet przez `node_modules` i mapę `exports`, czyli tą samą drogą co on. `externalDependencies` wymieniają esbuild (sondy szybkie) oraz `@angular/build` i `@angular/cli` (sonda prawdziwym builderem): podbicie któregokolwiek zmienia mierzone rozmiary, więc musi unieważnić cache — inaczej budżet pilnowałby liczby sprzed podbicia.",
  "// check-consumer": "Bramka konsumenta: odtwarza całą drogę od `npm pack` przez lokalny rejestr do przycisku pomalowanego tokenem w przeglądarce. Stoi w `components`, bo mierzy TEN pakiet. `dependsOn` na `build` i `schematics` jest twarde — pakuje artefakt razem z kolekcją `ng add`, a bez niej punkt 4 nie ma czego uruchomić. `inputs` wymieniają wyjście obu targetów, konfigurację Verdaccio (bramka publikuje przez nią, a nie przez własną kopię), skrypt i fixtures. `externalDependencies` są długie, bo długa jest droga: podbicie Verdaccio zmienia rejestr, podbicie `@angular/cli`/`@angular/build`/`@angular/ssr`/`@angular/router` zmienia build i renderowanie po stronie serwera, `express` niesie serwer aplikacji, a `@playwright/test` jest przeglądarką — każde z nich potrafi zmienić WYNIK pomiaru przy niezmienionym pakiecie, więc każde musi unieważniać cache. Bramka nie instaluje `peerDependencies` z rejestru: aplikacja bierze `@angular/*` z `node_modules` repozytorium przez wyszukiwanie w górę drzewa, tak samo jak sonda buildera w `check-bundle`. Cena jest zapisana wprost — rozjazd zakresu wersji w `peerDependencies` przejdzie tę bramkę i pilnuje go dopiero `wym-projekt-zaleznosci`.",
  "// check-texts": "Bramka kanału tekstów: pilnuje, że napis wypisywany przez bibliotekę idzie przez `PCT_TEXTS`. Stoi w `components`, bo wszystko, co czyta, leży w bibliotece i w jej `dist`. `dependsOn` na `build` jest twarde: punkt 2 porównuje odczyt ze źródeł z `ɵcmp.consts` i `ɵdir.hostAttrs` po zlinkowaniu, czyli z wynikiem prawdziwego kompilatora — bez tego atrybut wniesiony rozwinięciem obiektu w bloku `host` (`...fitHost` w `field/src/affix.ts`) nie miałby jak się pokazać. `inputs` wymieniają szablony i źródła WPROST, obok wyjścia builda: bramka czyta je bezpośrednio, a klucz cache ma wymieniać to, co czyta, a nie to, co zwykle zmienia się przy okazji (`lekcja-44`). `externalDependencies` wymieniają `@angular/compiler`, bo to jego `parseTemplate` i `TmplAstRecursiveVisitor` dają odczyt szablonu — podbicie Angulara może dołożyć rodzaj węzła, którego obejście nie zna, i to ma unieważnić cache, a nie przejść niezauważone.",
  "// mutacja": "Przebieg mutacyjny Strykera na rdzeniu (`core`, `[pctNumber]`, `PctSelect`) — jedyna maszyna odpowiadająca na pytanie, czy testy jednostkowe cokolwiek łapią (`wym-jakosc-jednostkowe`). Nie zależy od `build`: mutuje ŹRÓDŁA i uruchamia je własną konfiguracją Vitesta (`mutacja.vitest.config.mts`), bo target `test` idzie przez builder `@angular/build`, który pliku konfiguracyjnego nie ma i mieć nie może — składa go w pamięci. `inputs` wymieniają źródła i specyfikacje CAŁEJ biblioteki, nie samych plików mutowanych: mutant `select.ts` bywa zabijany testem `field.spec.ts`, więc zmiana dowolnej specyfikacji potrafi zmienić wynik. `externalDependencies` wymieniają Strykera z runnerem, Vitesta i wtyczkę Analoga: podbicie któregokolwiek zmienia zbiór mutantów albo sposób ich uruchomienia, więc musi unieważniać cache — inaczej próg pilnowałby liczby sprzed podbicia. Target sam faila poniżej `thresholds.break`; DLACZEGO to nie wystarcza, tłumaczy `check-mutation`.",
  "// check-mutation": "Bramka przebiegu mutacyjnego. Sam Stryker potrafi zapalić i domyślnie tego NIE robi — `thresholds.break` jest z definicji `null`, więc przebieg kończy się zerem przy dowolnym wyniku. A gdy próg już stoi, podnosi się go pięcioma ruchami, z których żaden nie dokłada testu: plik wykreślony z `mutate`, poszerzone `ignorers` albo `// Stryker disable` w źródle, wykluczona rodzina mutatorów, `ignoreStatic: true`, skrócony `timeoutMS` (mutant zabity ZEGAREM liczy się jak zabity asercją). Bramka czyta konfigurację SKUTECZNĄ z raportu, a nie z pliku — `--ignoreStatic` w poleceniu targetu nie zostawia w nim śladu. `inputs` biorą wyjście `mutacja`, politykę, snapshot, konfigurację Strykera, `project.json` (polecenie targetu) i workflow (punkt 7).",
  "// typecheck": "Trzy przebiegi, bo biblioteka ma trzy rozłączne programy: pakiet (`tsconfig.lib.json`), specyfikacje razem z `testing/` (`tsconfig.spec.json`) i schematics kompilowane do CommonJS (`schematics/tsconfig.json`). Żaden z nich nie widzi plików pozostałych dwóch, więc jedno wywołanie sprawdziłoby część biblioteki i wyglądałoby jak komplet — to `check-typecheck` porównuje tę sumę z listą plików z indeksu gita. `inputs` biorą `default`, a NIE `production`: `production` odejmuje `**/*.spec.ts`. Zmierzone, nie założone — dopisanie linii do `src/public-api.spec.ts` daje przy `default` `Cache: 0/1 hit`, a przy `production` `1/1 hit`, czyli drugi przebieg NIE rusza dokładnie wtedy, kiedy jest potrzebny (`lekcja-44` w kolejnym przebraniu). `build` sprawdza typy przy okazji, ale wyłącznie dla `tsconfig.lib.prod.json` — reszta nie ma innego kompilatora niż ten target.",
  "// stamp-version": "Przepisuje `version` z manifestu do stałej PCT_VERSION. Celowo NIE jest zależnością `build`: gdyby build sam się stemplował, artefakt zawsze by się zgadzał, a bramka `check-package` przestałaby cokolwiek badać. Uruchamia to wydanie (po podbiciu wersji) i człowiek — a bramka pilnuje, że nikt nie zapomniał.",
  "targets": {
    "themes": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": ["tokens:build"],
      "inputs": [{ "dependentTasksOutputFiles": "**/*" }],
      "outputs": ["{projectRoot}/themes"],
      "options": {
        "command": "mkdir -p themes && cp ../tokens/dist/pct.css ../tokens/dist/_tokens.scss themes/",
        "cwd": "libs/components"
      }
    },
    "build": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "defaultConfiguration": "production",
      "dependsOn": ["^build", "themes"],
      "options": {
        "project": "libs/components/ng-package.json",
        "tsConfig": "libs/components/tsconfig.lib.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "libs/components/tsconfig.lib.prod.json"
        },
        "development": {}
      }
    },
    "stamp-version": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "{projectRoot}/package.json",
        "{projectRoot}/stamp-version.mjs"
      ],
      "outputs": ["{projectRoot}/src/version.ts"],
      "options": {
        "command": "node libs/components/stamp-version.mjs"
      }
    },
    "schematics": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": ["build"],
      "inputs": ["{projectRoot}/schematics/**/*"],
      "outputs": ["{workspaceRoot}/dist/{projectRoot}/schematics"],
      "options": {
        "cwd": "libs/components",
        "commands": [
          "tsc -p schematics/tsconfig.json",
          "cp schematics/collection.json schematics/migration.json ../../dist/libs/components/schematics/",
          "cp schematics/ng-add/schema.json ../../dist/libs/components/schematics/ng-add/",
          "cp schematics/package.dist.json ../../dist/libs/components/schematics/package.json"
        ],
        "parallel": false
      }
    },
    "check-package": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": ["build", "schematics"],
      "inputs": [
        { "dependentTasksOutputFiles": "**/*", "transitive": false },
        "{projectRoot}/check-package.mjs",
        "{workspaceRoot}/tools/check-package.fixtures/**/*"
      ],
      "options": {
        "command": "node libs/components/check-package.mjs"
      }
    },
    "check-bundle": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": ["build"],
      "inputs": [
        { "dependentTasksOutputFiles": "**/*", "transitive": false },
        "{projectRoot}/**/ng-package.json",
        "{projectRoot}/rozmiar.snapshot.md",
        "{workspaceRoot}/tools/check-bundle.mjs",
        "{workspaceRoot}/tools/check-bundle.fixtures/**/*",
        {
          "externalDependencies": [
            "esbuild",
            "@angular/build",
            "@angular/cli",
            "@angular/compiler"
          ]
        }
      ],
      "options": {
        "command": "node tools/check-bundle.mjs"
      }
    },
    "check-consumer": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": ["build", "schematics"],
      "inputs": [
        { "dependentTasksOutputFiles": "**/*", "transitive": false },
        "{workspaceRoot}/.verdaccio/config.yml",
        "{workspaceRoot}/tools/check-consumer.mjs",
        "{workspaceRoot}/tools/check-consumer.fixtures/**/*",
        {
          "externalDependencies": [
            "verdaccio",
            "@angular/cli",
            "@angular/build",
            "@angular/ssr",
            "@angular/router",
            "express",
            "@playwright/test"
          ]
        }
      ],
      "options": {
        "command": "node tools/check-consumer.mjs"
      }
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "dist/{projectRoot}"
      }
    },
    "test": {
      "executor": "@nx/angular:unit-test",
      "outputs": ["{workspaceRoot}/coverage/{projectName}"],
      "options": {
        "watch": false,
        "tsConfig": "libs/components/tsconfig.spec.json",
        "include": ["**/*.spec.ts"],
        "coverage": true,
        "coverageInclude": [
          "libs/components/src/**/*.ts",
          "libs/components/src/**/*.html",
          "libs/components/*/src/**/*.ts",
          "libs/components/*/src/**/*.html"
        ],
        "coverageExclude": [
          "libs/components/**/*.spec.ts",
          "libs/components/**/*.types.ts",
          "libs/components/testing/**",
          "libs/components/src/version.ts"
        ],
        "coverageReporters": ["text", "json-summary"],
        "coverageThresholds": { "lines": 80 }
      }
    },
    "mutacja": {
      "executor": "nx:run-commands",
      "cache": true,
      "outputs": ["{workspaceRoot}/tmp/mutacja"],
      "inputs": [
        "{projectRoot}/**/*.ts",
        "{projectRoot}/**/*.html",
        "{projectRoot}/stryker.config.json",
        "{projectRoot}/mutacja.vitest.config.mts",
        "{workspaceRoot}/tsconfig.base.json",
        {
          "externalDependencies": [
            "@stryker-mutator/core",
            "@stryker-mutator/vitest-runner",
            "vitest",
            "@analogjs/vite-plugin-angular",
            "@analogjs/vitest-angular"
          ]
        }
      ],
      "options": {
        "command": "stryker run libs/components/stryker.config.json"
      }
    },
    "check-mutation": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": ["mutacja"],
      "inputs": [
        { "dependentTasksOutputFiles": "**/*", "transitive": false },
        "{projectRoot}/**/*.ts",
        "{projectRoot}/mutacja.policy.json",
        "{projectRoot}/mutacja.snapshot.md",
        "{projectRoot}/stryker.config.json",
        "{projectRoot}/project.json",
        "{workspaceRoot}/.github/workflows/ci.yml",
        "{workspaceRoot}/tools/check-mutation.mjs",
        "{workspaceRoot}/tools/check-mutation.fixtures/**/*"
      ],
      "options": {
        "command": "node tools/check-mutation.mjs"
      }
    },
    "check-coverage": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": ["test"],
      "inputs": [
        { "dependentTasksOutputFiles": "**/*", "transitive": false },
        "{projectRoot}/**/*.ts",
        "{projectRoot}/project.json",
        "{workspaceRoot}/tools/check-coverage.mjs",
        "{workspaceRoot}/tools/check-coverage.fixtures/**/*"
      ],
      "options": {
        "command": "node tools/check-coverage.mjs"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "default",
        "^production",
        { "externalDependencies": ["typescript"] }
      ],
      "options": {
        "parallel": false,
        "commands": [
          "tsc --noEmit -p libs/components/tsconfig.lib.json",
          "tsc --noEmit -p libs/components/tsconfig.spec.json",
          "tsc --noEmit -p libs/components/tsconfig.mutacja.json",
          "tsc --noEmit -p libs/components/schematics/tsconfig.json"
        ]
      }
    },
    "check-zoneless": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": ["build", "schematics"],
      "inputs": [
        { "dependentTasksOutputFiles": "**/*", "transitive": false },
        "{projectRoot}/*/src/**/*.ts",
        "{workspaceRoot}/**/package.json",
        "{workspaceRoot}/package-lock.json",
        "{workspaceRoot}/tools/check-zoneless.mjs",
        "{workspaceRoot}/tools/check-zoneless.fixtures/**/*"
      ],
      "options": {
        "command": "node tools/check-zoneless.mjs"
      }
    },
    "check-texts": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": ["build"],
      "inputs": [
        { "dependentTasksOutputFiles": "**/*", "transitive": false },
        "{projectRoot}/**/*.ts",
        "{projectRoot}/**/*.html",
        "{workspaceRoot}/tools/check-texts.mjs",
        "{workspaceRoot}/tools/check-texts.fixtures/**/*",
        { "externalDependencies": ["@angular/compiler"] }
      ],
      "options": {
        "command": "node tools/check-texts.mjs"
      }
    },
    "check-styles": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "{projectRoot}/**/*.scss",
        "{projectRoot}/**/*.ts",
        "{workspaceRoot}/tools/check-styles.mjs",
        "{workspaceRoot}/tools/check-styles.fixtures/**/*",
        { "externalDependencies": ["sass"] }
      ],
      "options": {
        "command": "node tools/check-styles.mjs"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
