{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "PacitComponentsNgAdd",
  "title": "Add @pacit/components to an application",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The project to wire the styles into. The workspace's default project by default.",
      "$default": {
        "$source": "projectName"
      }
    }
  },
  "required": []
}
