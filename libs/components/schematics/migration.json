{
  "$schema": "../../../node_modules/@angular-devkit/schematics/collection-schema.json",
  "//": "The `ng update` migration collection. It is empty, because the library has no released version to migrate from yet — but it exists from the start deliberately. The `ng-update` entry in the manifest has to point at a file present in the package from the first release: adding it only at the first breaking change will not help consumers who installed an earlier version, because `ng update` reads the collection from the INSTALLED version.",
  "schematics": {}
}
