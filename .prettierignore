# Add files here to ignore them from prettier formatting
/dist
/coverage

# Artifacts of the `check-tokens` reference control. They are in the repository against
# the `dist` rule in .gitignore because they carry their case's defect — but they are still
# GENERATOR OUTPUT, not hand-written code. Formatting them would make a fixture stop looking
# the way a real `dist` looks, which means measuring something else than it measures in repo.
/tools/check-tokens.fixtures/*/libs/tokens/dist
/.nx/cache
/.nx/workspace-data
.angular

.nx/self-healing