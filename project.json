{
  "name": "@org/source",
  "$schema": "node_modules/nx/schemas/project-schema.json",
  "// typecheck": "The root project has TypeScript files of its own — `vitest.config.ts` and `vitest.workspace.ts` — and until 2026-08-05 no compiler saw them, because they belong to neither a library nor an application. Same defect as `lesson-42`, one floor up: there a project went untypechecked, here the gap BETWEEN projects did. The configuration lives in `tsconfig.root.json`, not `tsconfig.json`: a file under the latter name at the root takes over the whole repository in the editor and in every tool that searches up the tree.",
  "// check-typecheck": "The typecheck gate walks the whole graph, so like `check-docs` it lives in the root project. `inputs` name EVERYTHING it reads: TypeScript files (the denominator), tsconfigs (the compiler programs) and `project.json`, `package.json`, `nx.json` (the graph, including targets inferred by plugins). The patterns are global because a new project has to be covered from its first commit — a hard-coded list would drift at the first new library, and the gate would be the one to stop seeing, not CI to fire.",
  "// check-tokens": "The token gate (names, tiers, pairs) lives in the root project rather than in `tokens`, and not for convenience: it reads package ENTRYPOINTS (`libs/components/*/ng-package.json`) and the library STYLESHEETS, while `tokens` is a dependency of `components` and not the other way round — so a change in the library does not make the tokens project affected. A removed entrypoint with `component.<name>.json` left behind would slip past the gate built partly for that. The root project is affected by every change (measured in `lesson-47`), so this gate always runs. `dependsOn` on the tokens build is hard: the gate compares ARTIFACTS in `dist` against the DTCG sources and without them has nothing to compare. Stylesheets are in `inputs` for point 7: it asks what the library REALLY paints, so a new `background:` declaration has to invalidate this cache — otherwise the first unmeasured color would ride through on a cache hit.",
  "// check-parts": "The parts inventory gate lives in the root project for two reasons at once. First it reads `docs/components/*.md` — the **Parts** tables are the only surface where a consumer ever sees that inventory — and `docs/` belongs to no library, so a target in `components` would not move when only a card changes. Second, the root project is affected by EVERY change (measured in `lesson-47`), and this gate guards the public styling API: the day it does not run is the day a part disappears without a trace. `dependsOn` on the library build is hard — the second reading of the inventory takes `ɵcmp` from the built package, not from sources, and without it has nothing to compare.",
  "// check-forms": "The form-control contract gate lives in the root project like the library's other structural gates: it is affected by every change, and what it holds breaks silently — every member of `FormUiControl` is optional, so a control missing one, or declaring one without its transform, compiles and passes every test the control already has. `inputs` name the three things it reads and one it does not own: the component sources (the controls and their bases), the block policy, and `@angular/forms` as an `externalDependencies` entry, because the contract's member list is read from the DEPENDENCY's own declarations — an Angular bump that adds or renames a member has to invalidate this cache rather than be answered from a run taken before it. No `dependsOn`: everything measured is in the sources, and a built package says nothing about which inputs a class declares.",
  "// check-icons": "The icon gate lives in the root project like the other gates over the library's templates: it is affected by every change, and the promise it holds is one that breaks silently — an `<svg>` written straight into a template renders perfectly and cannot be swapped. `inputs` name both sides of point 5: the templates (what is drawn) and `icon/src/icon.ts` (the published list of names), plus the script and its fixtures. `externalDependencies` names `@angular/compiler`, because `parseTemplate` is what reads the templates — an Angular bump can change what the walk sees, and that is to invalidate the cache rather than pass unnoticed. No `dependsOn`: everything measured here is in the sources, and an icon in the artifact says nothing about whether a consumer can replace it.",
  "// check-browsers": "The browser matrix gate lives in the root project although it is about `sandbox-e2e`, and not for convenience: it reads `.github/workflows/ci.yml` (point 5 — which engines the workflow installs and whether `e2e` is on the list at all), and a workflow belongs to no application. A target in `sandbox-e2e` would not move when only CI changes, leaving it blind to exactly the way of shrinking the matrix that leaves no trace in the Playwright configuration. The root project is affected by EVERY change (measured in `lesson-47`), so the gate always runs. `inputs` name everything it reads: the e2e configuration and policy, the spec files (the denominator of point 3), the workflow and `project.json` (the `e2e` target is INFERRED by `@nx/playwright/plugin`, so its command comes from the graph, not from a file). No `dependsOn`: the measurement goes through `playwright test --list`, which starts neither a server nor a browser — and the probe in point 6 asks about engine behaviour, not about this library.",
  "// check-e2e": "The e2e race gate: a number or an index standing where a condition belonged (`lesson-192`). It lives in the root project and NOT in `sandbox-e2e`, for the reason `check-browsers` gives one line up and one step further: its denominator is TWO applications at once — `apps/sandbox-e2e/src` and `apps/docs-e2e/src` — so a target inside either would be blind to half of it, and `nx affected` would leave it unrun on the day only the other moved. The root project is affected by every change (measured in `lesson-47`), so the gate always runs. `inputs` name exactly what it reads: the TypeScript of both suites — the specs it rules on and the `support/` trees the reader, wait and action vocabularies are derived from, which is why the pattern is `**/*.ts` and not `**/*.spec.ts` — plus the script and its fixtures. There are no `externalDependencies`: the gate is hand-written, it parses the specs itself rather than asking Playwright, and its only outside reader is `git ls-files`, which is how it asks what the repository really carries rather than what happens to be lying on the disk. No `dependsOn` either — everything measured is in the sources, and a passing e2e run says nothing about whether a test that passed measured the code or the machine.",
  "// check-language": "The language gate lives in the root project because its denominator is the WHOLE git index — no library owns that, and a target inside one would be blind to every file outside it. Hence `inputs` of `{workspaceRoot}/**/*`: any file at all can carry a second language, so any change has to invalidate this cache. `dependsOn` on the library build is hard — the public-surface limb reads `dist/libs/components`, and a translated source with a stale artifact is exactly the state it exists to catch. One thing it CANNOT hash: `/usr/share/dict/polish` and `/usr/share/dict/american-english` live outside the workspace, so a changed dictionary does not invalidate a cached run. The canaries of point 1 answer for that on every real run — but a cache hit executes nothing, so the honest statement is that the dictionaries are trusted between runs and measured on each one.",
  "// check-reach": "The reach gate walks the WHOLE git index from a list of roots, so it lives in the root project for the same reason `check-language` does: no library owns that denominator, and a target inside one would be blind to every file outside it. Hence `inputs` of `{workspaceRoot}/**/*` — a file added anywhere is a file to reach, and a line deleted anywhere can be the last thing that pointed at one, so any change has to invalidate this cache. No `dependsOn`: the measurement is over the repository, not over anything built from it — the walk reads sources, configuration and documentation, and `dist` is not tracked.",
  "// check-support": "The support policy gate lives in the root project because it reads `docs/support.md` — a document belonging to no library, so a target inside `components` would not move when only the policy changes, which is exactly the drift point 2 exists to catch. It is the one gate here with `cache: false`, and for a reason no `inputs` list can fix: point 4 reads GIT — the release tags and the commit subjects since the newest one — and git history is not a file pattern. Amending a message to add the `!` of a breaking change touches no file at all, so a cached run would answer from a measurement taken before the breaking change existed. The run is a handful of file reads and two `git` calls, so the price of always running it is smaller than the price of listing what it reads and being wrong.",
  "// check-distance": "The distance gate is the second here with `cache: false`, and for the same reason as `check-support` one line up, carried one step further: what it measures is not in any file. It counts the commits reachable from `HEAD` that no remote-tracking ref carries, and the age of the oldest of them — a reading that changes with every commit AND with every hour, while the files it would hash sit still. A cached pass is the distance on the day it was cached, which is precisely the defect the gate exists to catch: twelve days in which nothing grew except the distance (position 4.55). It lives in the root project because no library owns a repository's relationship to its remote, and it costs three `git` calls. Off a runner the reading is zero by construction; what it catches there is a checkout with no remote-tracking ref, which is the base `nx affected` compares against.",
  "// check-flake": "The flake gate is the third here with `cache: false`, and the reason is upstream of caching: what it reads is not in the repository. Its input is a pair of Playwright reports written by the repetition job in `nightly.yml`, into `tmp/`, on the runner that produced them — an `inputs` list naming them would hash a directory that is empty on every other machine, and a cache hit would answer a question about a run that has not happened. It lives in the root project because its denominator is TWO applications at once, `sandbox-e2e` and `docs-e2e`, the same reason `check-e2e` gives: a target inside either would be blind to half the measurement. It is deliberately NOT on the `nx affected` line of `ci.yml` — there are no reports there to read, and a gate that cannot measure must not be asked to.",
  "// check-index": "The index gate lives in the root project because the lists it measures belong to no library: `docs/decisions/`, the map on the first page of the documentation, and the README of every `check-*.fixtures/` tree in `tools/` and in `libs/components/`. Its `inputs` name both sides of each comparison — a list drifts when EITHER side moves, so the gate has to be invalidated by a new decision file as much as by an edit to the index, and by a gate growing a numbered point as much as by the sentence that counts them. Prettier is deliberately not among them: it shapes what `--write` writes, and the checking run compares cells, so a bump of the formatter cannot change this gate's answer.",
  "// check-docs": "The documentation gate: every promise in docs/ names a gate and its reference control, and the named paths exist and run in CI. It lives in the root project because `docs/` belongs to no library — which is what makes a documentation change mark this project affected. The comment sits HERE rather than in `targets`: there a value must be a target object, and a string blows up the Nx graph (`lesson-43`).",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "{workspaceRoot}/*.ts",
        "{workspaceRoot}/*.mts",
        "{workspaceRoot}/*.cts",
        "{workspaceRoot}/tsconfig.root.json",
        "{workspaceRoot}/tsconfig.base.json",
        {
          "externalDependencies": ["typescript"]
        }
      ],
      "options": {
        "command": "tsc --noEmit -p tsconfig.root.json"
      }
    },
    "check-typecheck": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": [
        {
          "projects": "tokens",
          "target": "build"
        }
      ],
      "inputs": [
        "{workspaceRoot}/**/*.ts",
        "{workspaceRoot}/**/*.mts",
        "{workspaceRoot}/**/*.cts",
        "{workspaceRoot}/**/tsconfig*.json",
        "{workspaceRoot}/**/project.json",
        "{workspaceRoot}/**/package.json",
        "{workspaceRoot}/nx.json",
        "{workspaceRoot}/tools/check-typecheck.mjs",
        "{workspaceRoot}/tools/check-typecheck.fixtures/**/*",
        {
          "externalDependencies": ["typescript"]
        }
      ],
      "options": {
        "command": "node tools/check-typecheck.mjs"
      }
    },
    "check-tokens": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": [
        {
          "projects": "tokens",
          "target": "build"
        }
      ],
      "inputs": [
        "{workspaceRoot}/libs/tokens/src/**/*.json",
        "{workspaceRoot}/libs/tokens/build.mjs",
        "{workspaceRoot}/libs/tokens/tokens.snapshot.md",
        "{workspaceRoot}/libs/components/*/ng-package.json",
        "{workspaceRoot}/libs/components/**/*.scss",
        "{workspaceRoot}/tools/check-tokens.mjs",
        "{workspaceRoot}/tools/check-tokens.fixtures/**/*",
        {
          "externalDependencies": ["sass", "@angular/cdk"]
        },
        {
          "dependentTasksOutputFiles": "**/*"
        }
      ],
      "options": {
        "command": "node tools/check-tokens.mjs"
      }
    },
    "check-bridge": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "{workspaceRoot}/libs/tokens/src/**/*",
        "{workspaceRoot}/libs/tokens/bridge.mjs",
        "{workspaceRoot}/tools/check-bridge.mjs",
        "{workspaceRoot}/tools/check-bridge.fixtures/**/*"
      ],
      "options": {
        "command": "node tools/check-bridge.mjs"
      }
    },
    "check-parts": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": [
        {
          "projects": "components",
          "target": "build"
        }
      ],
      "inputs": [
        "{workspaceRoot}/libs/components/**/*.ts",
        "{workspaceRoot}/libs/components/**/*.html",
        "{workspaceRoot}/libs/components/parts.snapshot.md",
        "{workspaceRoot}/docs/components/*.md",
        "{workspaceRoot}/tools/check-parts.mjs",
        "{workspaceRoot}/tools/check-parts.fixtures/**/*",
        {
          "dependentTasksOutputFiles": "**/*"
        }
      ],
      "options": {
        "command": "node tools/check-parts.mjs"
      }
    },
    "check-aria": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "{workspaceRoot}/libs/components/*/src/**/*.ts",
        "{workspaceRoot}/libs/components/*/src/**/*.html",
        "{workspaceRoot}/docs/components/*.md",
        "{workspaceRoot}/tools/check-aria.mjs",
        "{workspaceRoot}/tools/check-aria.fixtures/**/*",
        { "externalDependencies": ["axe-core", "@angular/compiler"] }
      ],
      "options": {
        "command": "node tools/check-aria.mjs"
      }
    },
    "check-forms": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "{workspaceRoot}/libs/components/*/src/**/*.ts",
        "{workspaceRoot}/libs/components/forms.policy.json",
        "{workspaceRoot}/tools/check-forms.mjs",
        "{workspaceRoot}/tools/check-forms.fixtures/**/*",
        {
          "externalDependencies": ["@angular/forms"]
        }
      ],
      "options": {
        "command": "node tools/check-forms.mjs"
      }
    },
    "check-icons": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "{workspaceRoot}/libs/components/*/src/**/*.html",
        "{workspaceRoot}/libs/components/icon/src/icon.ts",
        "{workspaceRoot}/tools/check-icons.mjs",
        "{workspaceRoot}/tools/check-icons.fixtures/**/*",
        {
          "externalDependencies": ["@angular/compiler"]
        }
      ],
      "options": {
        "command": "node tools/check-icons.mjs"
      }
    },
    "check-browsers": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "{workspaceRoot}/apps/sandbox-e2e/playwright.config.mts",
        "{workspaceRoot}/apps/sandbox-e2e/browsers.policy.json",
        "{workspaceRoot}/apps/sandbox-e2e/src/**/*.ts",
        "{workspaceRoot}/apps/sandbox-e2e/project.json",
        "{workspaceRoot}/.github/workflows/ci.yml",
        "{workspaceRoot}/tools/check-browsers.mjs",
        "{workspaceRoot}/tools/check-browsers.fixtures/**/*",
        {
          "externalDependencies": ["@playwright/test", "playwright"]
        }
      ],
      "options": {
        "command": "node tools/check-browsers.mjs"
      }
    },
    "check-e2e": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "{workspaceRoot}/apps/sandbox-e2e/src/**/*.ts",
        "{workspaceRoot}/apps/docs-e2e/src/**/*.ts",
        "{workspaceRoot}/tools/check-e2e.mjs",
        "{workspaceRoot}/tools/check-e2e.fixtures/**/*"
      ],
      "options": {
        "command": "node tools/check-e2e.mjs"
      }
    },
    "check-language": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": [
        {
          "projects": "components",
          "target": "build"
        }
      ],
      "inputs": [
        "{workspaceRoot}/**/*",
        "{workspaceRoot}/tools/check-language.mjs",
        "{workspaceRoot}/tools/language.policy.json",
        "{workspaceRoot}/tools/check-language.fixtures/**/*",
        {
          "dependentTasksOutputFiles": "**/*"
        }
      ],
      "options": {
        "command": "node tools/check-language.mjs"
      }
    },
    "check-reach": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "{workspaceRoot}/**/*",
        "{workspaceRoot}/tools/check-reach.mjs",
        "{workspaceRoot}/tools/reach.policy.json",
        "{workspaceRoot}/tools/check-reach.fixtures/**/*"
      ],
      "options": {
        "command": "node tools/check-reach.mjs"
      }
    },
    "check-support": {
      "executor": "nx:run-commands",
      "cache": false,
      "inputs": [
        "{workspaceRoot}/docs/support.md",
        "{workspaceRoot}/libs/components/package.json",
        "{workspaceRoot}/libs/components/schematics/**/*",
        "{workspaceRoot}/libs/components/*/src/**/*.ts",
        "{workspaceRoot}/libs/components/src/**/*.ts",
        "{workspaceRoot}/tools/check-support.mjs",
        "{workspaceRoot}/tools/check-support.fixtures/**/*"
      ],
      "options": {
        "command": "node tools/check-support.mjs"
      }
    },
    "check-acr": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "{workspaceRoot}/docs/acr/**/*",
        "{workspaceRoot}/docs/acr.md",
        "{workspaceRoot}/docs/components/*.md",
        "{workspaceRoot}/docs/plan.md",
        "{workspaceRoot}/docs/lessons.md",
        "{workspaceRoot}/docs/00-axis.md",
        "{workspaceRoot}/docs/requirements/*.md",
        "{workspaceRoot}/docs/decisions/*.md",
        "{workspaceRoot}/apps/sandbox-e2e/src/**/*.ts",
        "{workspaceRoot}/libs/components/*/src/**/*",
        "{workspaceRoot}/libs/components/package.json",
        "{workspaceRoot}/libs/tokens/src/**/*",
        "{workspaceRoot}/tools/check-*.mjs",
        "{workspaceRoot}/tools/check-acr.fixtures/**/*",
        "{workspaceRoot}/.github/workflows/ci.yml"
      ],
      "options": {
        "command": "node tools/check-acr.mjs"
      }
    },
    "check-index": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "{workspaceRoot}/docs/decisions/*.md",
        "{workspaceRoot}/docs/README.md",
        "{workspaceRoot}/docs/00-axis.md",
        "{workspaceRoot}/docs/requirements/*.md",
        "{workspaceRoot}/docs/lessons.md",
        "{workspaceRoot}/tools/check-*.mjs",
        "{workspaceRoot}/tools/*.fixtures/**/*",
        "{workspaceRoot}/libs/components/check-*.mjs",
        "{workspaceRoot}/libs/components/*.fixtures/**/*"
      ],
      "options": {
        "command": "node tools/check-index.mjs"
      }
    },
    "check-prose": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "{workspaceRoot}/tools/*.mjs",
        "{workspaceRoot}/tools/prose.policy.json",
        "{workspaceRoot}/tools/check-prose.fixtures/**/*",
        "{workspaceRoot}/docs/plan.md",
        "{workspaceRoot}/docs/prose.snapshot.md"
      ],
      "options": {
        "command": "node tools/check-prose.mjs"
      }
    },
    "check-distance": {
      "executor": "nx:run-commands",
      "cache": false,
      "options": {
        "command": "node tools/check-distance.mjs"
      }
    },
    "check-flake": {
      "executor": "nx:run-commands",
      "cache": false,
      "options": {
        "command": "node tools/check-flake.mjs"
      }
    },
    "check-tools": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "{workspaceRoot}/**/*.mjs",
        "{workspaceRoot}/tools/check-tools.mjs",
        "{workspaceRoot}/tools/check-tools.fixtures/**/*"
      ],
      "options": {
        "command": "node tools/check-tools.mjs"
      }
    },
    "check-docs": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "{workspaceRoot}/docs/**/*.md",
        "{workspaceRoot}/tools/check-docs.mjs",
        "{workspaceRoot}/tools/check-docs.fixtures/**/*.md",
        "{workspaceRoot}/.github/workflows/ci.yml"
      ],
      "options": {
        "command": "node tools/check-docs.mjs"
      }
    },
    "local-registry": {
      "executor": "@nx/js:verdaccio",
      "options": {
        "port": 4873,
        "config": ".verdaccio/config.yml",
        "storage": "tmp/local-registry/storage"
      }
    }
  }
}
